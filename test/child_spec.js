var expect = require('chai').expect;

describe('Specs', function () {
    describe('Child spec', function() {
        it('should work', function() {
            expect(true).to.be.truthly;
        });
    });
});
