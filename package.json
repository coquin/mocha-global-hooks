{
  "name": "mocha-test",
  "version": "1.0.0",
  "description": "A quick demo to test global `before` and `after` hooks in mocha.js",
  "scripts": {
    "test": "grunt test"
  },
  "dependencies": {
    "chai": "^3.4.1",
    "grunt": "^0.4.5",
    "grunt-mocha-test": "^0.12.7",
    "mocha": "^2.3.3"
  }
}
